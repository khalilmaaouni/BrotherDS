Status: CURRENT. Pointer written 2026-08-22 by the merger-analysis session (eab3d639, BrotherModeUp). Roles only, no dashes.

# Pointer: the roadmap of 2026-08-23, and what it decides for BrotherDS

The plan of record for the three products is `docs/plan/ROADMAP-2026-08-23-REPLAN.md` in the BrotherModeUp repository, with `docs/plan/MERGER-INVENTORY-2026-08-23.md` and `docs/plan/ADR-2026-08-23-one-brother-repository.md` beside it. This product is the third package of the one Brother repository that the founder named on 2026-08-22, joined after a six-condition timing gate, never before.

Founder decisions of 2026-08-22 that answer items in this repository's queue (`docs/plan/QUEUE.json`):

- `passport-consumer-authorised`: GRANTED. The siblings permit a second, read-only consumer of the change passport. This reader drops its UNAUTHORISED cap once the sentence lands in both seam contracts (phase 2 item P3 of the roadmap).
- `handoff-wire-format`: GRANTED, written by the assurance product as `contracts/handoff-package.v1.json` with a fixture and a test; this reader consumes the `ratified` marker (phase 2 item P4).
- Decision 5 (separate internal context, public MIT): YES, as a precondition of joining the public repository (phase 2 item P11, and timing gate condition 3): the nine name-bearing tracked files rewritten to roles, the research file whose name carries two terms renamed, claims and receipts kept out, then a clean extraction because the history keeps the names.
- Decision 3 (who owns the chain's verified-reality stage): DECIDED 2026-08-22 after the comparison the founder asked for, his option verbatim: "Split by unit, ledger owned by BrotherDS (Recommended)". BrotherMode records a change's outcome, BrotherSBE judges a change's verified reality (N2 stands), BrotherDS owns the stage's LEDGER: the Verified Claim Rate for claims and a change-level reopen rate computed from the siblings' records. It judges no change and writes no change record.
- Decision 6 (the vault space): DECIDED 2026-08-22, his option verbatim: "Propose one space 10-Projects/brother at the merge, keep three until then (Recommended)". Nothing is created before his yes at the merge.
- Decisions 1 (the path) and 2 (the boundary question): still the founder's; listed as phase 1 item 1.9 in the roadmap.
- Round four, 2026-08-22, his words verbatim: "Merge them under Brother new repo" (the target is a NEW repository named Brother, by clean extraction) and "Find the right approach as Fable and handle a harmony between all functions to be seamless like Superpowers" (delegated). The approach recorded in the ADR's round four section: the packaging stays three whole plugins in the Brother repository; a harmony layer makes them one product to the person using it (one entry point that routes, one vocabulary with the assertion object this repository's own one-product page identified, the seams as plumbing); phase 5 shrinks the combined surface by at least a third; and this product's capability is routed as EXPERIMENTAL until its first claim is scored against a real outcome (timing gate condition 7, a promotion gate, the counter-argument of docs/ONE-PRODUCT-2026-08-22.md adopted). That page's companion, docs/COORDINATED-PLAN-2026-08-22.md, already lands where round three did on the reality owner: occupied per unit.

Also scheduled for this repository: PROJECT.md's status line brought current (it still says nothing pushed and no remote); a VERSION, CHANGELOG.md, CHECKSUMS.sha256 and `.claude-plugin/plugin.json` under the queue item `plugin-surface` (phase 2 item P7); one run against the canonical passport fixture (P5).
